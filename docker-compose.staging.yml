# Optional on staging EC2: join Audittool Docker network (not required for API — gateway is on host :8080).
services:
  frontend-vue:
    networks:
      - default
      - audittool_net

networks:
  audittool_net:
    external: true
    name: ${AUDIT_NETWORK:-audittool_default}
